#ifndef PBSAMOA_HTSCODECS_CONFIG_H
#define PBSAMOA_HTSCODECS_CONFIG_H

/* Minimal static config for vendored htscodecs. Optional features are
 * enabled via compiler defines in Meson (e.g. HAVE_LIBBZ2). */

#endif  /* PBSAMOA_HTSCODECS_CONFIG_H */
